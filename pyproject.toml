[project]
name = "example-tasks"
version = "0.0.1"
description = "Fake tasks package"
readme = "README.md"
authors = [{ name = "Your Name", email = "first.last@example.com" }]
requires-python = ">=3.11,<3.15"
dependencies = ["pydantic>=2.6.0", "fractal-task-tools==0.4.0a0"]

[build-system]
requires = ["setuptools >= 80.0"]
build-backend = "setuptools.build_meta"
