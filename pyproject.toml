[project]
name = "example-tasks"
version = "0.0.1"
description = "Fake tasks package"
readme = "README.md"
authors = [{ name = "Your Name", email = "first.last@example.org" }]
requires-python = ">=3.11,<3.15"
dependencies = ["pydantic>=2.11.0", "fractal-task-tools==0.5.0a4"]

[build-system]
requires = ["setuptools >= 80.0"]
build-backend = "setuptools.build_meta"
